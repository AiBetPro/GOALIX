'use client';

const timeline = [
  { minute: '12′', event: 'But de Arsenal', team: 'Arsenal', detail: 'Saka ouvre le score après une action rapide sur le côté droit.' },
  { minute: '34′', event: 'Carton jaune', team: 'Chelsea', detail: 'Enzo Fernandez est averti pour un geste défensif tardif.' },
  { minute: '58′', event: 'Pressing haut', team: 'Arsenal', detail: 'Le pressing permet de récupérer le ballon en zone adverse.' },
  { minute: '67′', event: 'Remplacement', team: 'Chelsea', detail: 'Change tactique pour reprendre le contrôle du milieu.' },
];

const stats = [
  { label: 'Possession', value: '64%', team: 'Arsenal' },
  { label: 'Tirs cadrés', value: '6', team: 'Arsenal' },
  { label: 'Corners', value: '8', team: 'Arsenal' },
  { label: 'XG', value: '2.14', team: 'Arsenal' },
  { label: 'Passes clés', value: '21', team: 'Arsenal' },
  { label: 'Fautes', value: '9', team: 'Chelsea' },
];

const odds = [
  { label: '1', value: '1.65' },
  { label: 'N', value: '3.70' },
  { label: '2', value: '4.90' },
];

const trends = [
  'Arsenal domine le match au milieu de terrain.',
  'Chelsea a du mal à créer dans les phases de transition.',
  'Le pressing haut de Arsenal devient très dangereux à partir de la 55e minute.',
];

export default function MatchDetailPage() {
  return (
    <main className="goalix-match-page">
      <div className="match-detail-shell">
        <header className="goalix-ai-header glass-panel">
          <div className="brand-block">
            <div className="brand-mark">G</div>
            <div className="brand-text">
              GOA<span>LIX</span>
            </div>
          </div>

          <nav className="dashboard-nav desktop-nav">
            <a href="/dashboard">Dashboard</a>
            <a href="/live">Live</a>
            <a href="/ai-prono">IA Prono</a>
            <a href="/bets">Bet slip</a>
          </nav>

          <button className="profile-pill" aria-label="Profil">👤</button>
        </header>

        <section className="match-hero glass-panel">
          <div className="match-hero-top">
            <div>
              <span className="eyebrow-row">
                <span className="status-dot" />
                Premier League
              </span>
              <h1>Arsenal vs Chelsea</h1>
            </div>

            <div className="live-status-box">
              <span className="dot-live" />
              EN DIRECT
            </div>
          </div>

          <div className="match-score-block">
            <div className="team-score-card">
              <div className="team-mark home">A</div>
              <div>
                <strong>Arsenal</strong>
                <small>Home</small>
              </div>
            </div>

            <div className="score-box">
              <span>2</span>
              <small>:</small>
              <span>1</span>
            </div>

            <div className="team-score-card alt">
              <div className="team-mark away">C</div>
              <div>
                <strong>Chelsea</strong>
                <small>Away</small>
              </div>
            </div>
          </div>

          <div className="match-meta-row">
            <span>67′</span>
            <span>Emirates Stadium</span>
            <span>Samedi, 18:00</span>
          </div>
        </section>

        <section className="match-main-grid">
          <div className="match-side-stack">
            <div className="match-panel glass-panel">
              <div className="section-head compact">
                <div>
                  <span className="eyebrow">Analyse</span>
                  <h2>Statistiques</h2>
                </div>
              </div>

              <div className="stat-grid">
                {stats.map((stat) => (
                  <div key={stat.label} className="stat-card">
                    <small>{stat.label}</small>
                    <strong>{stat.value}</strong>
                    <span>{stat.team}</span>
                  </div>
                ))}
              </div>
            </div>

            <div className="match-panel glass-panel">
              <div className="section-head compact">
                <div>
                  <span className="eyebrow">Progression</span>
                  <h2>Temps forts</h2>
                </div>
              </div>

              <div className="timeline-list">
                {timeline.map((item) => (
                  <div key={item.minute} className="timeline-item">
                    <span className="timeline-time">{item.minute}</span>
                    <div className="timeline-wrap">
                      <strong>{item.event}</strong>
                      <small>{item.team}</small>
                      <p>{item.detail}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </div>

          <aside className="match-aside-stack">
            <div className="match-panel glass-panel">
              <div className="section-head compact">
                <div>
                  <span className="eyebrow">Cotes</span>
                  <h2>Marché principal</h2>
                </div>
              </div>

              <div className="odds-grid match-odds-box">
                {odds.map((odd) => (
                  <button key={odd.label} type="button" className="match-odds-button">
                    <small>{odd.label}</small>
                    <strong>{odd.value}</strong>
                  </button>
                ))}
              </div>
            </div>

            <div className="match-panel glass-panel">
              <div className="section-head compact">
                <div>
                  <span className="eyebrow">AI</span>
                  <h2>Analyse GOALIX</h2>
                </div>
              </div>

              <div className="insight-box">
                <div className="ai-signal">82%</div>
                <p>Le profil de jeu d’Arsenal reste plus fort dans le jeu direct et le pressing. L’écart tactique est net en faveur des locaux.</p>
              </div>

              <ul className="trend-list">
                {trends.map((trend) => (
                  <li key={trend}>{trend}</li>
                ))}
              </ul>
            </div>
          </aside>
        </section>

        <section className="bottom-actions">
          <button type="button" className="primary-dashboard-btn large-btn">Ajouter au coupon</button>
          <button type="button" className="secondary-dashboard-btn large-btn">Voir le calendrier</button>
        </section>
      </div>
    </main>
  );
}

























































