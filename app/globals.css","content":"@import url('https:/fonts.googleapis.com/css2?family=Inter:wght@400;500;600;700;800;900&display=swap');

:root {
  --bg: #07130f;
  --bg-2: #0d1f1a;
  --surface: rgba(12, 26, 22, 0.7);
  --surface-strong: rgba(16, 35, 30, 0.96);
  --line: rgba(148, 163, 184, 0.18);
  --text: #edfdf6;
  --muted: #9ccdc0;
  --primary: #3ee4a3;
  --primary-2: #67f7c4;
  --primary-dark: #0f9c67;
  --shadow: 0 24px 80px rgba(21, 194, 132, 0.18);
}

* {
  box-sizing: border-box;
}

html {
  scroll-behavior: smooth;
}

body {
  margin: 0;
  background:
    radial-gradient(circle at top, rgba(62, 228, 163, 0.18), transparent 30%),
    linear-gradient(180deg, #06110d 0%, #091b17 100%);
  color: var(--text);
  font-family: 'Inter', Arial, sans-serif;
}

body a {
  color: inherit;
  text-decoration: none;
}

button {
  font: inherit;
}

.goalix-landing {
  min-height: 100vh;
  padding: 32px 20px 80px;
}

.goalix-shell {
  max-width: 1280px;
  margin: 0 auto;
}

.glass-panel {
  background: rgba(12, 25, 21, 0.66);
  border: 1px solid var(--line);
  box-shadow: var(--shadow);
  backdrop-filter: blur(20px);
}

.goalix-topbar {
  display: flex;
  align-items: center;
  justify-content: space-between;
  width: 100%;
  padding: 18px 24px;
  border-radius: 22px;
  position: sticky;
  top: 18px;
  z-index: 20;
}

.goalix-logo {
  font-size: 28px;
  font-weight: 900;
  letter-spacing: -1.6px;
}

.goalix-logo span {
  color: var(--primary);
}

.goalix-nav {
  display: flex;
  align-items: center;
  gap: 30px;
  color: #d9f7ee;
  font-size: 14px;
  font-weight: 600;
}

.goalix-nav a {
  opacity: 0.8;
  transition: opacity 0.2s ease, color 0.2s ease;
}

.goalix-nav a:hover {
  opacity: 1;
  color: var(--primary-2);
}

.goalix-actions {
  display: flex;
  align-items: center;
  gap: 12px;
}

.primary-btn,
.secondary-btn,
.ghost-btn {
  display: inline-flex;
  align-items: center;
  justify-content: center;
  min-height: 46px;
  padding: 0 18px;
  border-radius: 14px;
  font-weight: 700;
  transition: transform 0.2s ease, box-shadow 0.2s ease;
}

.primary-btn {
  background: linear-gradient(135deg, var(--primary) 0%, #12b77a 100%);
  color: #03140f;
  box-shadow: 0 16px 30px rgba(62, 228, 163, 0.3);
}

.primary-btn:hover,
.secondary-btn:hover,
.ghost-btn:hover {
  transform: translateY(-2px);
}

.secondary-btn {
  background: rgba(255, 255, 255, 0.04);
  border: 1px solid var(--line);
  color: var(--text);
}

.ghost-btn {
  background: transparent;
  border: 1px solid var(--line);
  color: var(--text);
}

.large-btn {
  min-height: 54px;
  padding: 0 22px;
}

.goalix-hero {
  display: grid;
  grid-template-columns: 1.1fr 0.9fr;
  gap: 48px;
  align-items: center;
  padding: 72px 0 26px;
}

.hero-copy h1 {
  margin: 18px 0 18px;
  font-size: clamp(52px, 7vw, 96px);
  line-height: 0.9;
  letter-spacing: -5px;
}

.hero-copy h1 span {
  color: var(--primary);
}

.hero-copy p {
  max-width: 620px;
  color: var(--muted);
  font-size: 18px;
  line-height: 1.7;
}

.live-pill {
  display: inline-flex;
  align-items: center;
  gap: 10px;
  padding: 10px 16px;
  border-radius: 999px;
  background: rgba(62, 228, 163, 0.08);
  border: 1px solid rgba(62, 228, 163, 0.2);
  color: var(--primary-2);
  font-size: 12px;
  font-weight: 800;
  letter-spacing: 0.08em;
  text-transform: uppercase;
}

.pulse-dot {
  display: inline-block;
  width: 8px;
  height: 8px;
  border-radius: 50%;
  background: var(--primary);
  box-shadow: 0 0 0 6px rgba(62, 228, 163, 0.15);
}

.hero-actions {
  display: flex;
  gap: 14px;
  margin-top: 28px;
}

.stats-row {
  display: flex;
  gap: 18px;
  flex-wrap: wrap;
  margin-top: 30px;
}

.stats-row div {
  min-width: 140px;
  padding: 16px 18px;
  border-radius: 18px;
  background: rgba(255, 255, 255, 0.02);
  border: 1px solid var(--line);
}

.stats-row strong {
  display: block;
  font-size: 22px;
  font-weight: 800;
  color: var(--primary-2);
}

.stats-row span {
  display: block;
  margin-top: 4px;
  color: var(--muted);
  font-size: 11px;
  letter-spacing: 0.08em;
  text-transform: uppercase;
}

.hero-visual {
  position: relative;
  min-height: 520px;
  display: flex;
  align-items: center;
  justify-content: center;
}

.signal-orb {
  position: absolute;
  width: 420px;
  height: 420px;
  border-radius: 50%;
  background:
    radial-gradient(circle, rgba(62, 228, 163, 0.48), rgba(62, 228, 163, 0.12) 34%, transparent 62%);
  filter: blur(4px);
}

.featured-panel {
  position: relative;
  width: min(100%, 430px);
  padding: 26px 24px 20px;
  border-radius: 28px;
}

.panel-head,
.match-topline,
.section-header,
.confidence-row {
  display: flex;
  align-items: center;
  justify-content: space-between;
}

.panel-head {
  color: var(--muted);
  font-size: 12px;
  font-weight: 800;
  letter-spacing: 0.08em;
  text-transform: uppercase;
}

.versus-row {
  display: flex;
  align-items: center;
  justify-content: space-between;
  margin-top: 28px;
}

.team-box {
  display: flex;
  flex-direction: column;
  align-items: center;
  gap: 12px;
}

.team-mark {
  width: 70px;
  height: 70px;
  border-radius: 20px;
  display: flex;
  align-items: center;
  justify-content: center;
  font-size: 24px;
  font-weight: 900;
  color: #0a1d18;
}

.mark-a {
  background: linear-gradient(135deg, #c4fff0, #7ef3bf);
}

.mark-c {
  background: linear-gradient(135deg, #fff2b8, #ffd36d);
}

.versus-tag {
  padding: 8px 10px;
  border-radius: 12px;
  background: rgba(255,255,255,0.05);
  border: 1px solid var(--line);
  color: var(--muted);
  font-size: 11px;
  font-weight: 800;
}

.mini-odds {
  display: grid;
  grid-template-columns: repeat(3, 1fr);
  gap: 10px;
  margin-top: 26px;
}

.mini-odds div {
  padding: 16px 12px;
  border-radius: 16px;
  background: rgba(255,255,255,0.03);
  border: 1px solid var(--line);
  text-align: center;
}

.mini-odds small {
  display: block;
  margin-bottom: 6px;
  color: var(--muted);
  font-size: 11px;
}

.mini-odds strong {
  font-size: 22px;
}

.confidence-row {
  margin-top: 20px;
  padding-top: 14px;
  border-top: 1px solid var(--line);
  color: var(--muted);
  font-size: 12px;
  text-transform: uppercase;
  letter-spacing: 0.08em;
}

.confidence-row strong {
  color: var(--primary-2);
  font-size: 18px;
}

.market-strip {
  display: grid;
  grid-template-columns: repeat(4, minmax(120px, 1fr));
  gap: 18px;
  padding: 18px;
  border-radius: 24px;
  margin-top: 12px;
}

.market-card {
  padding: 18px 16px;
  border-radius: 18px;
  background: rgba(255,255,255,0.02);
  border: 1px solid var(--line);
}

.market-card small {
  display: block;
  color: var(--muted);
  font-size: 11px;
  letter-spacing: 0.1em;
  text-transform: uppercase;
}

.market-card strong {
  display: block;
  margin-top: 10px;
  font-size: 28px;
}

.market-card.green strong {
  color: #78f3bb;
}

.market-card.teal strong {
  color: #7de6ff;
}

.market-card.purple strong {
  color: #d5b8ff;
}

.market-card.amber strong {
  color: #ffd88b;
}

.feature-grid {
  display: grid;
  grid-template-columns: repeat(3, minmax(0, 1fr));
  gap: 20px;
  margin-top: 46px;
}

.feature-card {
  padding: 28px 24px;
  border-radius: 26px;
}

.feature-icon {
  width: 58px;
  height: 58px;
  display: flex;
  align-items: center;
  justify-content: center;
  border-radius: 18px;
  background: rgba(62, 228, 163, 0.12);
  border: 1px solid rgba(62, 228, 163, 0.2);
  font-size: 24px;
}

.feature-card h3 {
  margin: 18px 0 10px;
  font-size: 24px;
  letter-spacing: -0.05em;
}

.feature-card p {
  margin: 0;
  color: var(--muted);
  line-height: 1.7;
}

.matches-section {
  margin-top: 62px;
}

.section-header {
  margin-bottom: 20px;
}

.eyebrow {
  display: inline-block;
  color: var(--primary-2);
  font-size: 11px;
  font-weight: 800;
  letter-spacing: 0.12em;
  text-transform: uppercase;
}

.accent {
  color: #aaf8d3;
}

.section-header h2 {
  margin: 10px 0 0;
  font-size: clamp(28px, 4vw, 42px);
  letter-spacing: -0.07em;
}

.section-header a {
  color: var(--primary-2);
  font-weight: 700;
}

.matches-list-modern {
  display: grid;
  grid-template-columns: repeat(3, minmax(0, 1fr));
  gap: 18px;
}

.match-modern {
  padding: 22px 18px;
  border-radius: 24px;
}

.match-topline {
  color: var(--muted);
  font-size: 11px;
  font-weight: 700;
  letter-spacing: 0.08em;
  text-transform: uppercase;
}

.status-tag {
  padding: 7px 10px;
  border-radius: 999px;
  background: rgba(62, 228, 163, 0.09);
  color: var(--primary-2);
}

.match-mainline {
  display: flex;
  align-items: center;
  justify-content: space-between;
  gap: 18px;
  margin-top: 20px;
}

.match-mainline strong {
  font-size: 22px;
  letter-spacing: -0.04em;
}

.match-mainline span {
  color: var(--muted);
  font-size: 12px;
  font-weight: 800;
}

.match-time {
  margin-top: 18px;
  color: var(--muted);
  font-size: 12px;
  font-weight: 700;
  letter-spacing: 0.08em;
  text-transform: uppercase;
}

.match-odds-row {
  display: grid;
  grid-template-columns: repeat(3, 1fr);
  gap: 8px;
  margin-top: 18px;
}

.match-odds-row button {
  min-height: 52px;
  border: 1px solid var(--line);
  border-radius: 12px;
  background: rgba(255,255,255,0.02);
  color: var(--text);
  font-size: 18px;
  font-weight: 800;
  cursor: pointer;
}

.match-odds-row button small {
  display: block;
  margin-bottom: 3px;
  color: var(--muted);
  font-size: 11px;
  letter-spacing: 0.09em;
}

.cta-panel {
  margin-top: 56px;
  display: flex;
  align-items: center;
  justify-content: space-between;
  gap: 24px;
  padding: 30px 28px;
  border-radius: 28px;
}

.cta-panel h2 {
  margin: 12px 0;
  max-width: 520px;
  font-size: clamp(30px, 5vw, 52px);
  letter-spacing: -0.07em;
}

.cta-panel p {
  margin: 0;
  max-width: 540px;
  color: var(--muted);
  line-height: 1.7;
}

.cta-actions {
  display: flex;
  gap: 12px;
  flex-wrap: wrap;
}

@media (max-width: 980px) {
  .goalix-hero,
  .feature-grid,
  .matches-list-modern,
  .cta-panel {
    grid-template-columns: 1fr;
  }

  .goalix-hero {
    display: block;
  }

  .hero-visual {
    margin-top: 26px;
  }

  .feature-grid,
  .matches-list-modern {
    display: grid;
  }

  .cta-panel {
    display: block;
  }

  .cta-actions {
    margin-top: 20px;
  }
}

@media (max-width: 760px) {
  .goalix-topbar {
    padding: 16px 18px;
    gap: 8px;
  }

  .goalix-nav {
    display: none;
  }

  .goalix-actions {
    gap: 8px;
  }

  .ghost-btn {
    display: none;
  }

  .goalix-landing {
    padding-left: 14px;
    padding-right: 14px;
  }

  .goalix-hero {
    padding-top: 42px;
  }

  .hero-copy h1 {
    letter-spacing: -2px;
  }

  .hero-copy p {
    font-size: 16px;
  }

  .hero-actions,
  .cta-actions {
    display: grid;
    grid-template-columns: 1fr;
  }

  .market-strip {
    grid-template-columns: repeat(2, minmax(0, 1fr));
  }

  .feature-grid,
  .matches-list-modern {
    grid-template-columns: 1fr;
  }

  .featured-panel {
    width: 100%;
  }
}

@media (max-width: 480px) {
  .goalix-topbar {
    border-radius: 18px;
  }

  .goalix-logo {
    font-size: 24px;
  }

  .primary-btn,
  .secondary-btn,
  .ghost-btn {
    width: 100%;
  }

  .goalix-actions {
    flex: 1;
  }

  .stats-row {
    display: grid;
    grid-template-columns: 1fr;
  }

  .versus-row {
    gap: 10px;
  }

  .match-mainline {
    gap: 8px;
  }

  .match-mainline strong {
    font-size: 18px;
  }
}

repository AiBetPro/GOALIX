import Link from 'next/link';

const matches = [
  {
    league: 'Premier League',
    time: '18:00',
    home: 'Arsenal',
    away: 'Chelsea',
    odds: ['1.65', '3.70', '4.90'],
    status: 'Live',
  },
  {
    league: 'La Liga',
    time: '20:30',
    home: 'Barcelona',
    away: 'Sevilla',
    odds: ['1.42', '4.40', '6.80'],
    status: 'Trending',
  },
  {
    league: 'Serie A',
    time: '21:00',
    home: 'Inter',
    away: 'Milan',
    odds: ['1.75', '3.50', '4.30'],
    status: 'AI pick',
  },
];

const features = [
  {
    title: 'Live odds intelligence',
    text: 'Suivez les mouvements de cotes en temps réel et repérez les opportunités avant le marché.',
    icon: '⚡',
  },
  {
    title: 'AI prediction engine',
    text: 'Analyse des stats, formes, tendances et probabilités pour une lecture plus claire du match.',
    icon: '🤖',
  },
  {
    title: 'Smart bet slip',
    text: 'Construit ton coupon proprement, compare les marchés et garde le contrôle de ton bankroll.',
    icon: '🎯',
  },
];

const markets = [
  { name: '1X2', value: '1.86', glow: 'green' },
  { name: 'Over 2.5', value: '1.92', glow: 'teal' },
  { name: 'BTTS', value: '1.74', glow: 'purple' },
  { name: 'Corners', value: '2.11', glow: 'amber' },
];

export default function Home() {
  return (
    <main className="goalix-landing">
      <div className="goalix-shell">
        <header className="goalix-topbar glass-panel">
          <Link href="/" className="goalix-logo">
            GOA<span>LIX</span>
          </Link>

          <nav className="goalix-nav desktop-nav">
            <Link href="/dashboard">Sports</Link>
            <Link href="/live">Live</Link>
            <Link href="/ai-prono">AI Prono</Link>
            <Link href="/bets">Bet slip</Link>
          </nav>

          <div className="goalix-actions">
            <button className="ghost-btn">Se connecter</button>
            <Link href="/dashboard" className="primary-btn">
              Ouvrir l’app
            </Link>
          </div>
        </header>

        <section className="goalix-hero">
          <div className="hero-copy">
            <div className="live-pill">
              <span className="pulse-dot" />
              Sports intelligence • AI powered
            </div>

            <h1>
              Analyse <span>sportive</span>
              <br />
              plus rapide.
              <br />
              Plus intelligente.
            </h1>

            <p>
              GOALIX combine données live, cotes et IA pour t’aider à lire les matchs,
              comparer les marchés et construire des décisions plus solides.
            </p>

            <div className="hero-actions">
              <Link href="/dashboard" className="primary-btn large-btn">
                Voir les matchs
              </Link>
              <Link href="/ai-prono" className="secondary-btn large-btn">
                IA Prono
              </Link>
            </div>

            <div className="stats-row">
              <div>
                <strong>1.2M+</strong>
                <span>points de données</span>
              </div>
              <div>
                <strong>24/7</strong>
                <span>live tracking</span>
              </div>
              <div>
                <strong>92%</strong>
                <span>signalisation utile</span>
              </div>
            </div>
          </div>

          <div className="hero-visual">
            <div className="signal-orb" />

            <div className="featured-panel glass-panel">
              <div className="panel-head">
                <span>Match phare</span>
                <small>18:00</small>
              </div>

              <div className="versus-row">
                <div className="team-box">
                  <div className="team-mark mark-a">A</div>
                  <strong>Arsenal</strong>
                </div>

                <div className="versus-tag">VS</div>

                <div className="team-box">
                  <div className="team-mark mark-c">C</div>
                  <strong>Chelsea</strong>
                </div>
              </div>

              <div className="mini-odds">
                <div>
                  <small>1</small>
                  <strong>1.65</strong>
                </div>
                <div>
                  <small>X</small>
                  <strong>3.70</strong>
                </div>
                <div>
                  <small>2</small>
                  <strong>4.90</strong>
                </div>
              </div>

              <div className="confidence-row">
                <span>AI confidence</span>
                <strong>82%</strong>
              </div>
            </div>
          </div>
        </section>

        <section className="market-strip glass-panel">
          {markets.map((market) => (
            <div key={market.name} className={`market-card ${market.glow}`}>
              <small>{market.name}</small>
              <strong>{market.value}</strong>
            </div>
          ))}
        </section>

        <section className="feature-grid">
          {features.map((feature) => (
            <article key={feature.title} className="feature-card glass-panel">
              <div className="feature-icon">{feature.icon}</div>
              <h3>{feature.title}</h3>
              <p>{feature.text}</p>
            </article>
          ))}
        </section>

        <section className="matches-section">
          <div className="section-header">
            <div>
              <span className="eyebrow">Trending now</span>
              <h2>Matchs populaires</h2>
            </div>
            <Link href="/live">Voir tout →</Link>
          </div>

          <div className="matches-list-modern">
            {matches.map((match) => (
              <article key={`${match.home}-${match.away}`} className="match-modern glass-panel">
                <div className="match-topline">
                  <span>{match.league}</span>
                  <span className="status-tag">{match.status}</span>
                </div>

                <div className="match-mainline">
                  <div>
                    <strong>{match.home}</strong>
                  </div>
                  <span>VS</span>
                  <div>
                    <strong>{match.away}</strong>
                  </div>
                </div>

                <div className="match-time">{match.time}</div>

                <div className="match-odds-row">
                  {match.odds.map((odd, index) => (
                    <button key={`${match.home}-${index}`} type="button">
                      <small>{index === 0 ? '1' : index === 1 ? 'X' : '2'}</small>
                      {odd}
                    </button>
                  ))}
                </div>
              </article>
            ))}
          </div>
        </section>

        <section className="cta-panel glass-panel">
          <div>
            <span className="eyebrow accent">GOALIX AI</span>
            <h2>La meilleure lecture de match en un seul endroit.</h2>
            <p>
              Gagne du temps, surveille les marchés et décide avec plus de confiance.
            </p>
          </div>

          <div className="cta-actions">
            <Link href="/ai-prono" className="primary-btn large-btn">
              Découvrir l’IA
            </Link>
            <Link href="/dashboard" className="secondary-btn large-btn">
              Explorer les matchs
            </Link>
          </div>
        </section>
      </div>
    </main>
  );
}

